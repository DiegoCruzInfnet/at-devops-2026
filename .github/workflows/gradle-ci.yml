# ============================================
# PARTE 2 - Workflow 3: Build Maven
# Disparo: push na branch main
# ============================================

name: Maven CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: devops2026-ufoTracker
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Instalar Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      
      - name: Permissão ao mvnw
        run: chmod +x mvnw

      - name: Build com Maven
        run: ./mvnw clean package -DskipTests
      
      - name: Upload do artefato JAR
        uses: actions/upload-artifact@v4
        with:
          name: ufo-tracker-jar
          path: devops2026-ufoTracker/target/*.jar
