# ============================================
# PARTE 2 - Workflow 2: Testes
# Disparo: pull_request
# ============================================

name: Testes

on:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Rodando testes
        run: echo "Rodando testes"
